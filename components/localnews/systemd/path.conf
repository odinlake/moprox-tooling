# Managed by moprox-tooling. Without this, systemd PATH omits ~/.local/bin and the claude CLI
# is unreachable — the failure mode that scored 1,200 posts as 0 for three weeks.
[Service]
Environment=PATH=/home/mikael/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
