# Poll every 5 minutes. `systemctl enable --now metrics-collect.timer` on the collector host.
[Unit]
Description=Collect Proxmox metrics every 5 minutes

[Timer]
OnCalendar=*:0/5
Persistent=true

[Install]
WantedBy=timers.target
