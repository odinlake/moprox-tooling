[Unit]
Description=Sweep private-data for uncommitted producer output, hourly
[Timer]
OnCalendar=hourly
# Jitter so the sweep does not race the mail lane's own nightly commit at the top of the hour.
RandomizedDelaySec=300
Persistent=true
[Install]
WantedBy=timers.target
